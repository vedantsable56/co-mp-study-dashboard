const co_mp_data = {
  "subjectId": "co_mp",
  "subjectName": "Computer Organization and Microprocessor",
  "units": [
    {
      "unitNum": 1,
      "unitTitle": "Computer Evolution and Performance",
      "questions": [
        {
          "id": "co_mp_q1",
          "number": 1,
          "question": "Difference between Von Neumann and Harvard Architecture",
          "marks": 7,
          "answer": "| Comparison Parameter | Von Neumann Architecture | Harvard Architecture |\n| :--- | :--- | :--- |\n| **1. Memory Structure** | Uses a unified memory space that stores both instruction opcodes and operand data, sharing the same physical storage media. | Utilizes separate, physically isolated memory modules for storing instructions (Program Memory) and data (Data Memory). |\n| **2. Bus Configuration** | Uses a single system bus (shared address and data buses) to access both instruction codes and operand data from memory. | Features independent, dedicated buses (instruction address/data bus and data address/data bus) running in parallel. |\n| **3. Execution Speed** | Slower, as instruction fetches and data read/write operations must be serialized over the shared bus, causing the Von Neumann bottleneck. | Faster, as the CPU can fetch a machine instruction and perform a memory read/write operation simultaneously in one clock cycle. |\n| **4. Pipelining Efficiency** | Hard to implement efficiently because the instruction fetch stage frequently conflicts with the operand read/write stage over the shared bus. | Highly compatible with multi-stage pipelining since instruction fetching and data access execute on separate, non-conflicting buses. |\n| **5. Hardware Complexity** | Simpler hardware interface design, requiring fewer address/data pins on the physical CPU chip and less complex motherboard routing. | Complex processor design, requiring double the number of address/data pins on the CPU and complex, dual-bus motherboard traces. |\n| **6. System Cost** | Lower overall production cost due to a simplified bus system, fewer CPU pins, and the requirement of only a single memory block. | Higher manufacturing cost due to duplicate memory chips, dual-bus trace layouts, and higher pin-count CPU packaging. |\n| **7. Code Security** | Lower security, as executing code and application data share memory, allowing program instructions to be accidentally overwritten. | Higher security, as program memory is write-protected and physically separated, preventing data operations from corrupting code. |\n| **8. Memory Utilization** | High optimization efficiency, since unused program memory space can be dynamically allocated to store application data and vice-versa. | Rigid memory allocation, where unused space in the program memory module cannot be utilized by data-heavy applications. |\n| **9. Control Unit Design** | Simpler control logic state machine, as it only needs to coordinate sequential, non-overlapping bus cycles. | Complex control unit design, requiring arbitration and synchronization logic to manage two parallel buses simultaneously. |\n| **10. Typical Applications** | General-purpose computing systems, such as desktop computers, laptops, and servers, where memory flexibility is essential. | High-speed digital signal processors (DSPs), microcontrollers (e.g., 8051, AVR), and real-time embedded systems. |"
        },
        {
          "id": "co_mp_q2",
          "number": 2,
          "question": "Draw and Explain the Hardware Implementation of Booth's Algorithm",
          "marks": 6,
          "answer": "### Definition\nThe hardware implementation of Booth's algorithm provides the physical registers, arithmetic logic unit (ALU), control circuitry, and datapath logic required to perform signed binary multiplication. It operates directly on numbers represented in two's complement form without requiring separate sign-detection or conversion hardware.\n\n### Diagram\n```\n                     ┌───────────────────────────────┐\n                     │         Control Logic         │\n                     │  (State Machine & Sequencer)  │\n                     └──────┬────────────────┬───────┘\n                            │                │\n            ┌─────────┐  ┌──▼────────────────▼┐\n            │Regis-   │  │    n-Bit ALU       │\n            │er M     │─►│ (Adder/Subtractor) │\n            │(Multi-  │  └────────┬───────────┘\n            │ plicand)│           │\n            └─────────┘           ▼\n                        ┌──────────────────┐   ┌─────────┐   ┌───────┐\n                        │   Register A     │──►│Register │──►│Flip-  │\n                        │  (Accumulator)   │   │  Q      │   │Flop   │\n                        │                  │   │(Multi-  │   │Q_-1   │\n                        │                  │   │ plier)  │   │       │\n                        └──────────────────┘   └─────────┘   └───────┘\n                            │                      │             │\n                            └───────────────┬──────┴─────────────┘\n                                            ▼\n                                  Arithmetic Shift Right\n```\n\n### Components and Functions\n*   **Register M (Multiplicand Register)**: A dedicated n-bit register that stores the multiplicand value throughout the multiplication process. It provides its stored value to the ALU for addition or subtraction operations based on the control signals.\n*   **Register Q (Multiplier Register)**: An n-bit shift register that initially holds the multiplier. During each step of the algorithm, the least significant bit (Q_0) is scanned alongside Q_-1, and the entire register is shifted arithmetically to the right.\n*   **Register A (Accumulator Register)**: An n-bit register initialized to all zeros at the start of the operation. It stores the partial products generated during successive addition and subtraction cycles, eventually forming the upper half of the final product.\n*   **Flip-Flop Q_-1**: A single-bit register placed adjacent to the least significant bit of Register Q (Q_0). It is initialized to zero and holds the bit shifted out from Q_0 during the previous arithmetic right shift, allowing the control logic to check bit transitions.\n*   **n-Bit Parallel ALU (Adder/Subtractor)**: Performs addition (A + M) or subtraction (A - M) operations on the contents of Register A and Register M. The result of the operation is immediately written back into Register A.\n*   **Sequence Counter (SC)**: A hardware counter register initialized with the word size (n). It decrements by one at the end of each clock cycle (after the shift step) and signals the control logic to terminate the operation when its count reaches zero.\n*   **Control Logic Block**: A state machine that continuously samples the bits Q_0 and Q_-1. It generates the enable and select signals for the ALU (addition, subtraction, or no operation) and triggers the arithmetic right shift of the combined [A, Q, Q_-1] registers.\n\n### Working\nThe multiplication process starts by loading the multiplicand into Register M, the multiplier into Register Q, and clearing Register A and Flip-Flop Q_-1 to zero. The sequence counter (SC) is loaded with the bit-width n. During each of the n clock cycles, the control logic evaluates the binary state of the two bits [Q_0, Q_-1]:\n1. If the bits are `01`, Register M is added to Register A (A ← A + M) via the ALU.\n2. If the bits are `10`, Register M is subtracted from Register A (A ← A - M) via the ALU.\n3. If the bits are `00` or `11`, no arithmetic operation is performed.\nFollowing the arithmetic phase, an Arithmetic Shift Right (ASHR) is performed on the combined registers [A, Q, Q_-1]. This shift operation preserves the sign bit (MSB of Register A) to maintain the correct two's complement representation. The sequence counter (SC) is terminated when it reaches zero, at which point the final 2n-bit product is available in the combined registers [A, Q]."
        },
        {
          "id": "co_mp_q3",
          "number": 3,
          "question": "Discuss the Limitation of a Ripple Carry Adder and Explain How a Carry Look-Ahead (CLA) Adder Improves Speed",
          "marks": 7,
          "answer": "### Definition / Introduction\nA Ripple Carry Adder (RCA) is a digital circuit that adds two n-bit numbers by chaining n Full Adders in series, where the carry output of each stage is connected to the carry input of the succeeding stage. A Carry Look-Ahead (CLA) Adder is an advanced parallel design that reduces carry propagation delays by calculating the carry-in bits for all stages simultaneously using logic equations.\n\n### Diagram\n```\n  Ripple Carry Adder Propagation Delay:\n  A0 B0         A1 B1         A2 B2         A3 B3\n  ┌──┴──┐       ┌──┴──┐       ┌──┴──┐       ┌──┴──┐\n  │ FA0 ├──C1──►│ FA1 ├──C2──►│ FA2 ├──C3──►│ FA3 ├──C4──► (Sequential propagation)\n  └──┬──┘       └──┬──┘       └──┬──┘       └──┬──┘\n     S0            S1            S2            S3\n\n  Carry Look-Ahead Adder Logic Block:\n  A0,B0 ────► ┌─────────────────────────────────┐ ────► S0\n  A1,B1 ────► │  Carry Generate (G) &           ├─ C1-C3► S1\n  A2,B2 ────► │  Carry Propagate (P) Logic Block│ ────► S2\n  A3,B3 ────► └─────────────────────────────────┘ ────► S3 (Parallel calculation)\n```\n\n### Limitations of Ripple Carry Adder (RCA)\n*   **Carry Propagation Delay**: The primary limitation of the RCA is that the carry bit must \"ripple\" sequentially through every Full Adder stage from the least significant bit (LSB) to the most significant bit (MSB). No stage can compute its final sum and carry output until it receives the steady carry input from the previous stage.\n*   **Linear Time Complexity**: The propagation delay increases linearly with the bit-width n of the adders. If each Full Adder stage introduces a delay of t_d, the worst-case delay for an n-bit addition is n·t_d, which makes the circuit slow for 32-bit or 64-bit word sizes.\n*   **Performance Bottleneck**: In modern high-frequency arithmetic logic units (ALUs), the linear carry delay of the RCA restricts the maximum operating clock speed of the entire processor, making it unsuitable for high-performance computing.\n\n### How Carry Look-Ahead (CLA) Adder Improves Speed\nThe CLA adder improves speed by eliminating the dependency on sequential carry propagation. It defines two intermediate signals for each bit position:\n1. **Carry Generate (G_i)**: Active when a carry is generated at stage i regardless of the input carry.\n   Equation: G_i = A_i · B_i\n2. **Carry Propagate (P_i)**: Active when an input carry is propagated to the next stage.\n   Equation: P_i = A_i ⊕ B_i\n\nUsing these signals, the carry-in for every stage is calculated directly using parallel logic gates that look ahead to the carry status of the preceding stages:\n*   C_1 = G_0 + P_0 · C_0\n*   C_2 = G_1 + P_1 · G_0 + P_1 · P_0 · C_0\n*   C_3 = G_2 + P_2 · G_1 + P_2 · P_1 · G_0 + P_2 · P_1 · P_0 · C_0\n*   C_4 = G_3 + P_3 · G_2 + P_3 · P_2 · G_1 + P_3 · P_2 · P_1 · G_0 + P_3 · P_2 · P_1 · P_0 · C_0\n\nSince these equations depend only on the initial inputs (A_i, B_i) and the external carry-in (C_0), the carry logic block can calculate all carry bits in parallel. All carries are generated within two gate delays, reducing the time complexity from O(n) to a constant O(1), which improves the operating speed of large adders."
        },
        {
          "id": "co_mp_q4",
          "number": 4,
          "question": "Multiply Using Booth's Algorithm: Multiplicand = +3, Multiplier = -4",
          "marks": 8,
          "answer": "### Definition\nBooth's algorithm is an multiplication algorithm that multiplies two signed binary numbers in two's complement representation. It scans the multiplier bits to perform additions, subtractions, or shifts, reducing the total count of additions required.\n\n### Principle\nThe algorithm evaluates adjacent bit pairs of the multiplier (Q_0 and Q_-1) to determine the arithmetic operation. If the transition is `10` (falling edge), a subtraction is performed; if `01` (rising edge), an addition is performed; if `00` or `11`, no arithmetic operation is required. An arithmetic right shift is executed in every cycle to align the partial products.\n\n### Steps\n1. Initialize Register A to 0000, Flip-Flop Q_-1 to 0, and loaded Multiplicand M and Multiplier Q.\n2. Initialize the Sequence Counter (SC) to the bit-width n = 4.\n3. Inspect the combination of Q_0 and Q_-1.\n4. Execute A ← A + M (for 01) or A ← A - M (for 10) if required.\n5. Perform an Arithmetic Shift Right (ASHR) on [A, Q, Q_-1].\n6. Decrement SC by 1. If SC is not zero, repeat from Step 3.\n\n### Working and Numerical Trace\n- Multiplicand (M) = +3 = `0011` in binary.\n- Two's complement of Multiplicand (-M) = -3 = `1101` in binary.\n- Multiplier (Q) = -4 = `1100` in binary.\n- Initial Setup: A = `0000`, Q = `1100`, Q_-1 = `0`, SC = `4`.\n\n| Cycle | Accumulator (A) | Multiplier (Q) | Q_-1 | Q_0, Q_-1 | Performed Operation | SC |\n| :--- | :---: | :---: | :---: | :---: | :--- | :---: |\n| **Init** | 0000 | 1100 | 0 | - | Load registers and clear A, Q_-1 | 4 |\n| **C1** | 0000 | 1100 | 0 | `00` | No arithmetic operation; execute ASHR | 4 |\n| **Shift** | 0000 | 0110 | 0 | - | ASHR [A, Q, Q_-1]; sign bit preserved | 3 |\n| **C2** | 0000 | 0110 | 0 | `00` | No arithmetic operation; execute ASHR | 3 |\n| **Shift** | 0000 | 0011 | 0 | - | ASHR [A, Q, Q_-1]; sign bit preserved | 2 |\n| **C3** | 1101 | 0011 | 0 | `10` | Subtract M: execute A ← A + (-M) | 2 |\n| **Shift** | 1110 | 1001 | 1 | - | ASHR [A, Q, Q_-1]; sign bit preserved | 1 |\n| **C4** | 1110 | 1001 | 1 | `11` | No arithmetic operation; execute ASHR | 1 |\n| **Shift** | 1111 | 0100 | 1 | - | ASHR [A, Q, Q_-1]; sign bit preserved | 0 |\n\nThe final product is stored in the combined registers [A, Q] as `11110100`.\n- Verification: The binary value `11110100` is a signed two's complement number.\n- Inverting the bits: `00001011`\n- Adding 1: `00001100` (equal to decimal 12).\n- Since the original sign bit was 1, the result is -12, which is correct (+3 · -4 = -12).\n\n### Key Execution Steps\n*   **Multiplicand Registration**: The multiplicand value of +3 is represented in signed 4-bit binary as `0011` and stored in Register M. Its two's complement value `-M` is computed as `1101` and is utilized during subtraction cycles when the multiplier transitions from a `1` to a `0`.\n*   **Multiplier Registration**: The multiplier value of -4 is represented in signed 4-bit binary as `1100` and loaded into Register Q. The least significant bit (Q_0) and Flip-Flop Q_-1 are examined in each cycle to determine the arithmetic operations.\n*   **Registers Initialization**: Before the multiplication process begins, Register A (Accumulator) and Flip-Flop Q_-1 are initialized to `0000` and `0` respectively. The Sequence Counter (SC) is initialized to `4` (representing the bit-width of the operands).\n*   **Bit Evaluation and Operation**: In each cycle, the bit combination of [Q_0, Q_-1] determines whether addition, subtraction, or shift-only occurs. Subtraction is performed for state `10`, addition for `01`, and no arithmetic operation is executed for states `00` or `11`.\n*   **Arithmetic Right Shift (ASHR)**: An arithmetic right shift is performed on the combined registers [A, Q, Q_-1] at the end of each cycle. This shift operation preserves the sign bit (MSB of Register A) to ensure that the two's complement signed representation remains mathematically correct.\n\n### Advantages\n*   Supports signed operands in two's complement format directly without pre-conversion.\n*   Reduces the number of add/subtract operations when the multiplier contains strings of consecutive 1s or 0s.\n*   Simplifies datapath hardware requirements in signed digital multiplier designs."
        },
        {
          "id": "co_mp_q5",
          "number": 5,
          "question": "Define the Structure of a System Bus. Explain Data Bus, Address Bus, and Control Bus",
          "marks": 7,
          "answer": "### Definition / Introduction\nA system bus is a set of physical wiring traces on a computer motherboard that serves as a shared communication pathway connecting the central processing unit (CPU), system memory, and input/output (I/O) peripheral devices. It coordinates digital signal transmission to prevent bus conflicts.\n\n### Diagram\n```\n   ┌────────────────────────────────────────────────────────┐\n   │                       SYSTEM BUS                       │\n   │   ┌──────────────────────────────────────────────────┐ │\n   │   │  ADDRESS BUS (Unidirectional: CPU to Memory/IO)  │ │\n   │   ├──────────────────────────────────────────────────┤ │\n   │   │  DATA BUS    (Bidirectional: CPU ◄═► Memory/IO)  │ │\n   │   ├──────────────────────────────────────────────────┤ │\n   │   │  CONTROL BUS (Bidirectional: CPU ◄═► Memory/IO)  │ │\n   │   └──────────────────────────────────────────────────┘ │\n   └─────────┬───────────────────┬───────────────────┬──────┘\n             ▼                   ▼                   ▼\n       ┌───────────┐       ┌───────────┐       ┌───────────┐\n       │    CPU    │       │  MEMORY   │       │I/O Devices│\n       └───────────┘       └───────────┘       └───────────┘\n```\n\n### Detailed Explanation of Bus Components\n*   **Address Bus**: The address bus is a unidirectional set of lines that carries the physical address signals generated by the CPU to select a target memory location or memory-mapped I/O port. The width of the address bus determines the maximum size of the memory space that the CPU can access. For example, the 8086 has 20 address lines, enabling it to address up to 1 MB of memory.\n*   **Data Bus**: The data bus consists of bidirectional lines used to transfer machine instruction codes, variable data, and constants between the processor and memory or peripheral modules. The bit-width of the data bus determines how many bits can be fetched or written in a single bus cycle, which directly affects system performance. Common widths include 8, 16, 32, and 64 bits.\n*   **Control Bus**: The control bus contains multiple bidirectional lines that transmit control, command, timing, and status signals across the computer modules to coordinate bus activity. These signals prevent data collisions by ensuring only one device drives data onto the shared bus lines at any time. Important control signals include read/write requests, ready signals, and interrupts.\n*   **Bus Width and Capacity**: The physical number of lines in the address bus dictates the maximum memory locations (2^n) the processor can address, while the data bus lines determine the execution bandwidth. Increasing the bus widths improves data transmission rates and allows the CPU to access larger data structures in a single cycle.\n\n### Advantages / Features\n*   **Reduces Wiring Complexity**: Connects all hardware blocks through a single shared set of lines rather than complex, point-to-point connections.\n*   **Scalability**: Allows adding new peripheral cards or memory modules directly to the shared system bus without modifying the CPU interface.\n*   **Arbitration Controls**: Includes bus request and grant lines that enable multiple master devices, like DMA controllers, to share access."
        }
      ]
    },
    {
      "unitNum": 2,
      "unitTitle": "Memory Management",
      "questions": [
        {
          "id": "co_mp_q6",
          "number": 6,
          "question": "Explain the Difference Between Write-Through and Write-Back Policies",
          "marks": 6,
          "answer": "| Comparison Parameter | Write-Through Policy | Write-Back Policy |\n| :--- | :--- | :--- |\n| **1. Memory Update Timing** | Updates the cache line and the backing main memory location simultaneously during a write operation. | Updates only the cache line during a write, delaying the main memory write until the cache block is evicted. |\n| **2. Write Performance** | Has a higher write latency because the CPU must wait for the slower main memory write cycle to complete. | Has a lower write latency since write operations execute at high cache speeds without waiting for RAM. |\n| **3. System Bus Traffic** | Generates high bus traffic because every write operation triggers a write cycle on the system bus. | Generates low bus traffic, only utilizing the bus when modified blocks are written back on eviction. |\n| **4. Hardware Complexity** | Has low hardware complexity because no state tracking or dirty bits are needed in the cache directory. | Has high hardware complexity since it requires dirty bits, replacement controllers, and write-back buffers. |\n| **5. Data Consistency** | Guarantees strict consistency; main memory always holds the same data as the cache. | Allows temporary inconsistency; main memory may hold stale data until write-back occurs. |\n| **6. Write Buffer Need** | Requires a multi-word write buffer to prevent CPU stalls during back-to-back write operations. | Does not require a write buffer for standard write cycles, though a victim buffer is used during block replacement. |\n| **7. Directory Tag Overhead** | Requires no dirty bit storage overhead in the cache directory tag. | Requires one dirty bit per cache line to track modifications, increasing tag memory size. |\n| **8. Reliability and Recovery** | Offers high reliability; a power failure does not lose data since memory is always up to date. | Has lower reliability; dirty data in cache is lost if a system crash occurs before write-back. |\n| **9. Multi-core Consistency** | Simplifies consistency protocols because all write operations are visible on the shared bus. | Requires complex consistency protocols (like MESI) to invalidate or update stale copies in other caches. |\n| **10. Scalability** | Has poor scalability in multi-processor systems due to bus saturation from frequent memory writes. | Has high scalability because local cache writes minimize bus access, saving bandwidth. |"
        },
        {
          "id": "co_mp_q7",
          "number": 7,
          "question": "Compare the Three Cache Mapping Techniques: Direct, Associative, and Set-Associative",
          "marks": 7,
          "answer": "| Comparison Parameter | Direct Mapping | Fully Associative | Set-Associative |\n| :--- | :--- | :--- | :--- |\n| **1. Block Placement Rule** | A memory block maps to exactly one fixed line in the cache, determined by index bits. | A memory block can be placed in any cache line. | A memory block maps to a specific set, and can be placed in any line within that set. |\n| **2. Address Fields Partition** | Physical address is divided into Tag, Line Index, and Word Offset fields. | Physical address is divided only into Tag and Word Offset fields. | Physical address is divided into Tag, Set Index, and Word Offset fields. |\n| **3. Comparator Hardware** | Requires a single comparator to check the tag of the indexed cache line. | Requires multiple comparators (equal to the total cache lines) to search all tags in parallel. | Requires a number of comparators equal to the set associativity (k-way) to search the selected set. |\n| **4. Conflict Miss Rate** | Highest conflict miss rate due to multiple memory blocks mapping to the same line index. | Lowest conflict miss rate because any free cache line can hold the block. | Moderate conflict miss rate, which decreases as the associativity size (k) increases. |\n| **5. Access Delay (Hit Time)** | Fastest access time since index bits directly locate the only candidate cache line. | Slower hit time due to the propagation delay of parallel comparison across all tags. | Moderate hit time, combining index decoding and set-level tag comparison. |\n| **6. Design and Hardware Cost** | Lowest cost because of simple comparator logic and standard SRAM directory tags. | Highest hardware cost due to associative search logic and content-addressable memory (CAM). | Moderate hardware cost, balancing logical set-decoders and k comparators. |\n| **7. Replacement Algorithm** | No replacement algorithm is needed; new blocks overwrite the existing block at the mapped index. | Requires a replacement algorithm (like LRU, FIFO) to select a victim block when the cache is full. | Requires a replacement algorithm to select a victim block within the targeted set. |\n| **8. Cache Space Utilization** | Poor utilization since cache lines can be empty while other lines suffer constant swaps. | Maximum utilization because any block can occupy any line in the cache. | Good utilization, distributing blocks evenly across sets. |\n| **9. Thrashing Probability** | High probability of thrashing when a program accesses two blocks that map to the same index. | Zero thrashing probability because there are no index-based conflicts. | Low thrashing probability, as k blocks can map to the same set without conflicts. |\n| **10. Typical Application** | Primary L1 instruction caches where fast access time is critical. | Translation Lookaside Buffers (TLBs) and small specialized buffers. | Modern L1 data caches, L2, and L3 caches in standard processors. |"
        },
        {
          "id": "co_mp_q8",
          "number": 8,
          "question": "Write a Note on RAID and Its Levels in Detail with Diagram",
          "marks": 7,
          "answer": "### Definition / Introduction\nRAID (Redundant Array of Independent Disks) is a storage technology that combines multiple physical hard drives into a single logical unit. It distributes data across the drives to achieve data redundancy, improve performance, or both, protecting the system from data loss due to individual drive failures.\n\n### Diagram\n```\n              ┌──────────────────────────────────┐\n              │         RAID Controller          │\n              └──────┬────────────┬────────────┬─┘\n                     ▼            ▼            ▼\n                ┌─────────┐  ┌─────────┐  ┌─────────┐\n                │ Disk 0  │  │ Disk 1  │  │ Disk 2  │\n                └─────────┘  └─────────┘  └─────────┘\n```\n\n### Detailed Explanation of RAID Levels\n*   **RAID 0 (Data Striping)**: Splits data block-by-block across all physical drives in the array. It provides the highest read/write performance because operations are executed in parallel across multiple drives. However, it lacks redundancy, and the failure of a single drive results in complete data loss.\n*   **RAID 1 (Data Mirroring)**: Writes identical copies of all data to two or more disks simultaneously. It offers high data safety and can survive the failure of one disk, but it has a high cost overhead because the usable storage space is reduced to 50% of the total capacity.\n*   **RAID 5 (Distributed Parity)**: Stripes data blocks across three or more drives and distributes parity calculations across all disks. It balances storage efficiency with reliability, allowing the array to survive a single drive failure while losing only the capacity of one disk to parity.\n*   **RAID 6 (Dual Distributed Parity)**: Extends RAID 5 by using two independent parity calculations (P and Q) distributed across a minimum of four drives. This dual-parity configuration allows the array to survive the simultaneous failure of any two hard drives, providing maximum reliability for critical storage systems.\n*   RAID 2, 3, and 4 (Specialized Levels):\n    *   *RAID 2* stripes data at the bit level and uses Hamming code ECC stored on dedicated parity drives to detect and correct single-bit errors.\n    *   *RAID 3* stripes data at the byte level and uses a single dedicated parity disk to rebuild data in case of a drive failure.\n    *   *RAID 4* stripes data at the block level and uses a single dedicated parity disk, creating a write bottleneck on the parity drive.\n\n### Advantages / Features\n*   **Fault Tolerance**: Provides data redundancy across multiple drives to prevent system downtime and data loss when hardware failures occur.\n*   **High Performance**: Increases input/output speeds by striping data blocks and accessing multiple disk channels in parallel.\n*   **Storage Aggregation**: Combines multiple small physical disks into one large logical volume, simplifying volume management for the operating system."
        },
        {
          "id": "co_mp_q9",
          "number": 9,
          "question": "Describe LRU, FIFO, and LFU Replacement Algorithms",
          "marks": 7,
          "answer": "### Definition / Introduction\nCache replacement algorithms are protocols used by the cache controller to select which memory block to evict when the cache is full and a new block must be loaded. These algorithms aim to maximize the cache hit rate by keeping active blocks and evicting blocks that are unlikely to be referenced in the near future.\n\n### Diagram\n```\n  FIFO (Queue):     [New Block] ──► [ Tail ] ──► [ Head ] ──► [ Evicted Block ]\n  LRU (Stack):      [Most Recent] ─────────────────► [Least Recent (Evicted)]\n  LFU (Counters):   [Freq: 10] , [Freq: 8] , [Freq: 2 (Evicted Block)]\n```\n\n### Detailed Explanation of Replacement Algorithms\n*   FIFO (First-In, First-Out) Algorithm:\n    *   *Logic*: Tracks the order in which blocks enter the cache and evicts the oldest block that has been in the cache for the longest duration.\n    *   *Operation*: Implemented using a simple queue structure where new blocks are added to the tail and evicts occur at the head.\n    *   *Characteristics*: Low implementation overhead, but it can suffer from Belady's anomaly where increasing cache size increases miss rates. It may evict frequently used blocks simply because of their age.\n*   LRU (Least Recently Used) Algorithm:\n    *   *Logic*: Evicts the block that has not been accessed for the longest period of time.\n    *   *Operation*: Relies on temporal locality, updating timestamps or moving elements in a hardware stack on every hit.\n    *   *Characteristics*: Provides a high hit rate by keeping recently accessed data, but it requires significant hardware overhead (counters or stack logic) to update metadata on every clock cycle.\n*   LFU (Least Frequently Used) Algorithm:\n    *   *Logic*: Tracks how many times each block is accessed and evicts the block with the lowest total hit count.\n    *   *Operation*: Maintains a counter register associated with each cache line, incrementing the counter on every block access.\n    *   *Characteristics*: Keeps popular blocks in the cache, but it suffers from cache pollution when old blocks accumulate high frequency counts and remain in the cache after they are no longer needed.\n\n### Key Features\n*   **Temporal Locality Support**: LRU takes advantage of temporal locality by assuming that recently accessed addresses are highly likely to be accessed again soon.\n*   **Hardware Overhead**: FIFO has the lowest hardware overhead, while LRU and LFU require additional storage bits and logic gates to track access statistics.\n*   **Hit Rate Optimization**: Selecting the correct replacement policy helps the processor maintain high speeds by avoiding slow main memory access cycles."
        },
        {
          "id": "co_mp_q10",
          "number": 10,
          "question": "Physical Components of a Hard Disk and Define Access Time, Seek Time, Rotational Delay",
          "marks": 8,
          "answer": "### Definition\nA magnetic Hard Disk Drive (HDD) is a non-volatile storage device that stores digital data on rotating platters coated with a magnetic material. It uses electromagnetic read/write heads to access data stored in concentric circular tracks on the platter surfaces.\n\n### Diagram\n```\n        Platter Layout                      Side View\n     ┌────────────────┐                 ┌───────────────┐\n    ╱                  ╲                │ Actuator Arm  │\n   │     Tracks (O)     │               │  ┌──────────┐ │\n   │    ┌──────────┐    │               │  ├─ R/W Head│ │──► Platter 0\n   │    │Sector ▰  │    │               │  ├─ R/W Head│ │──► Platter 1\n   │    │   (●)    │    │               └─┬───────────┘\n   │    │ Spindle  │    │                 │\n   └────┴──────────┴────┘              Spindle Motor\n```\n\n### Components and Functions\n*   **Magnetic Platters**: Circular aluminum or glass disks coated with a magnetic material that stores binary data. Platters are stacked vertically on a spindle and rotate at high speeds (e.g., 5400 or 7200 RPM).\n*   **Spindle Motor**: A high-speed motor that rotates the platters at a constant speed, ensuring that the magnetic tracks move past the read/write heads at a stable rate.\n*   **Read/Write Heads**: Small electromagnetic sensors mounted on the actuator arms. They float micro-inches above the platter surfaces, reading magnetic fields to detect data or writing fields to store data.\n*   **Actuator Arm**: A mechanical arm that supports and moves the read/write heads radially across the rotating platters to position them over the targeted tracks.\n*   **Tracks and Sectors**: Platters are divided into concentric circular paths called *Tracks*, which are further divided into radial segments called *Sectors*. The sector is the smallest addressable unit of physical data storage on the disk (typically 512 bytes or 4 KB).\n\n### Working and Access Time Parameters\nTo read or write a sector, the actuator arm positions the head over the correct track, and the platter rotates to align the sector under the head. The total delay before data transfer begins is defined as **Access Time**, which is calculated using the following components:\n\nTotal Access Time = Seek Time + Rotational Delay + Transfer Time\n\n*   **Seek Time (T_seek)**: The mechanical delay required to move the actuator arm and position the read/write head over the target track. This is the slowest component of the access time because it involves physical movement.\n*   **Rotational Delay (T_rotational)**: The delay required for the target sector to rotate under the read/write head. The average rotational delay is calculated as half the time for one complete rotation:\n    Average Rotational Delay = 1/2 · 60/RPM  seconds\n*   **Transfer Time (T_transfer)**: The time required to read or write the actual data bits from/to the disk sector once the head is aligned. It depends on the disk's recording density and rotational speed:\n    Transfer Time = \\frac{Data Bytes to Transfer}{Data Transfer Rate}"
        }
      ]
    },
    {
      "unitNum": 3,
      "unitTitle": "Introduction to 8086 Microprocessor",
      "questions": [
        {
          "id": "co_mp_q11",
          "number": 11,
          "question": "Draw the Pin Diagram of 8086 and Explain ALE, WR̅, DEN̅ and DT/R̅",
          "marks": 6,
          "answer": "### Definition\nThe pin configuration of the 8086 microprocessor is a standard 40-pin Dual In-line Package (DIP) layout. To reduce the package size, the processor uses multiplexed address and data bus lines (AD0–AD15), requiring external hardware support and dedicated control signals to separate the address and data phases during a bus cycle.\n\n### Diagram\n```\n                     ┌────────────┐\n         GND  [1]  ──┤            ├──  [40] VCC (+5V)\n     AD14     [2]  ──┤            ├──  [39] AD15\n     AD13     [3]  ──┤            ├──  [38] A16/S3\n     AD12     [4]  ──┤            ├──  [37] A17/S4\n     AD11     [5]  ──┤            ├──  [36] A18/S5\n     AD10     [6]  ──┤    8086    ├──  [35] A19/S6\n     AD9      [7]  ──┤            ├──  [34] BHE̅/S7\n     AD8      [8]  ──┤    CPU     ├──  [33] MN/MX̅\n     AD7      [9]  ──┤            ├──  [32] RD̅\n     AD6      [10] ──┤            ├──  [31] HOLD\n     AD5      [11] ──┤            ├──  [30] HLDA\n     AD4      [12] ──┤            ├──  [29] WR̅\n     AD3      [13] ──┤            ├──  [28] M/IO̅\n     AD2      [14] ──┤            ├──  [27] DT/R̅\n     AD1      [15] ──┤            ├──  [26] DEN̅\n     AD0      [16] ──┤            ├──  [25] ALE\n     NMI      [17] ──┤            ├──  [24] INTA̅\n     INTR     [18] ──┤            ├──  [23] TEST̅\n     CLK      [19] ──┤            ├──  [22] READY\n     GND      [20] ──┤            └──────  [21] RESET\n                     └────────────┘\n```\n\n### Detailed Explanation of Pin Functions\n*   **ALE (Address Latch Enable)**: This is an active-high output signal generated by the CPU during the first clock state (T1) of every bus cycle. It signals external latch devices (such as the 74LS373 IC) to lock the valid address bits present on the multiplexed AD0–AD15 lines. When ALE drops low, the address is held in the latch, freeing the CPU lines to carry data.\n*   **WR̅ (Write)**: This is an active-low output control signal driven low by the processor during the T2 clock phase of a write bus cycle. It indicates that the CPU has placed valid data on the data bus lines, and it remains active through T3 to allow memory or I/O devices to write the data.\n*   **DEN̅ (Data Enable)**: This is an active-low output control signal used to enable external bidirectional data transceivers (such as the 74LS245 IC). It goes low during the T2 phase of read and write cycles to connect the CPU's internal data bus to the external system bus, preventing bus conflicts when address signals are active.\n*   **DT/R̅ (Data Transmit/Receive)**: This is an output control signal that defines the direction of data transmission through the external transceivers. When DT/R̅ is driven high, the transceivers transmit data from the CPU to the bus (write cycle); when driven low, they receive data from the bus to the CPU (read cycle).\n*   **BHE̅ (Bus High Enable)**: This is an active-low output signal used to enable the upper memory bank (connected to data lines D8–D15). Combined with address bit A0, it allows the processor to perform 8-bit transfers on the upper data lines, 8-bit transfers on the lower data lines, or 16-bit word transfers.\n*   **MN/MX̅ (Minimum/Maximum Mode)**: This is an input pin that configures the operating mode of the 8086 processor. When tied to VCC (+5V), the CPU operates in Minimum Mode (single-processor setup), generating its own control bus signals. When connected to GND, it operates in Maximum Mode (multiprocessor setup), relying on the external 8288 bus controller to generate control signals.\n\n### Key Features\n*   **Multiplexed Bus Architecture**: Combines address and data signals on the same physical lines to reduce the pin count of the processor package.\n*   **Tristate Capability**: Many pins support a high-impedance state, allowing external devices like DMA controllers to take control of the system bus."
        },
        {
          "id": "co_mp_q12",
          "number": 12,
          "question": "Explain Architecture of 8086",
          "marks": 7,
          "answer": "### Definition\nThe architecture of the 8086 microprocessor is split into two independent functional blocks: the **Bus Interface Unit (BIU)** and the **Execution Unit (EU)**. This modular division enables parallel execution of instructions (pipelining), as the BIU fetches instructions in advance while the EU decodes and executes the currently loaded instruction.\n\n### Labelled Diagram\n```\n  ┌──────────────────────────────────────────────────────────────────┐\n  │                         8086 CPU                                 │\n  │                                                                  │\n  │  ┌─────────────────────────┐        ┌─────────────────────────┐  │\n  │  │   Bus Interface Unit    │        │     Execution Unit      │  │\n  │  │  Segment Regs (CS, DS,  │        │  General Regs (AX, BX,  │  │\n  │  │  SS, ES) & IP           │        │  CX, DX, SP, BP, SI, DI)│  │\n  │  └──────────┬──────────────┘        └────────────▲────────────┘  │\n  │             │ 20-bit Address                     │               │\n  │             ▼                                    │               │\n  │       Address Adder (Σ)                     Control Unit         │\n  │             │                                    │               │\n  │       ┌─────▼─────┐                         ┌────┴────┐          │\n  │       │ 6-Byte    ├─ (Prefetched Opcodes) ─►│   ALU   │          │\n  │       │ Queue     │                         └─────────┘          │\n  │       └─────┬─────┘                                              │\n  └─────────────┼────────────────────────────────────────────────────┘\n                ▼ External System Bus\n```\n\n### Explanation of Architectural Blocks\n*   Bus Interface Unit (BIU):\n    *   *Segment Registers (CS, DS, SS, ES)*: These 16-bit registers hold the base addresses of four active memory segments (Code, Data, Stack, Extra), allowing the CPU to partition memory blocks.\n    *   *Instruction Pointer (IP)*: A 16-bit register holding the offset address of the next instruction byte to be fetched from the code segment.\n    *   *Address Generation Adder (Σ)*: An internal hardware adder that shifts the 16-bit segment address left by 4 bits and adds the 16-bit offset to produce a 20-bit physical address.\n    *   *6-Byte Instruction Queue*: A First-In-First-Out (FIFO) buffer that stores up to 6 bytes of prefetched instruction code, enabling parallel instruction fetching.\n*   Execution Unit (EU):\n    *   *Arithmetic Logic Unit (ALU)*: A 16-bit arithmetic and logic block that executes mathematical operations (ADD, SUB, MUL, DIV) and Boolean logical operations (AND, OR, XOR).\n    *   *General Purpose Registers (AX, BX, CX, DX)*: Registers used for data manipulation. They can be split into independent 8-bit registers (AH, AL, etc.) for byte operations.\n    *   *Pointer and Index Registers (SP, BP, SI, DI)*: Store offset addresses for stack access (SP, BP) and string manipulation indexing (SI, DI).\n    *   *Instruction Decoder & Control Unit*: Reads instruction bytes from the BIU queue, decodes the opcodes, and generates internal control signals to execute the operations.\n    *   *Flag Register*: A 16-bit register containing 9 active flags (6 status flags and 3 control flags) that record the results of ALU operations and define CPU operating modes.\n\n### Working of Architecture\nThe BIU and EU operate in parallel. When the system is initialized, the BIU calculates physical addresses, fetches instruction bytes from memory, and stores them in the 6-byte queue. The EU continuously pops decoded instructions from the queue and executes them. If the ALU requires memory access for operands, the EU requests the BIU to perform a read or write bus cycle. When a branch instruction (e.g., JMP or CALL) is executed, the EU flushes the instruction queue, and the BIU immediately fetches the instruction from the new target address, introducing a queue-refilling delay.\n\n### Key Features\n*   **Parallel Pipelining**: Prefetches up to 6 bytes of instruction codes during idle bus cycles, reducing instruction execution delays.\n*   **Segmented Memory Support**: Divides the 1 MB physical address space into logical segments, allowing relocatable code and data isolation."
        },
        {
          "id": "co_mp_q13",
          "number": 13,
          "question": "Explain Programmer's Model (Register Organization) of 8086",
          "marks": 7,
          "answer": "### Definition\nThe programmer's model of the 8086 microprocessor consists of 14 user-accessible 16-bit registers. These registers are organized into distinct functional categories to manage data processing, memory segmentation, stack operations, and processor status flag configurations.\n\n### Labelled Diagram\n```\n  AX: [  AH  ][  AL  ]   (Accumulator)     CS: [   Code Segment   ]\n  BX: [  BH  ][  BL  ]   (Base)            DS: [   Data Segment   ]\n  CX: [  CH  ][  CL  ]   (Count)           SS: [   Stack Segment  ]\n  DX: [  DH  ][  DL  ]   (Data)            ES: [   Extra Segment  ]\n\n  SP: [ Stack Pointer ]                    IP: [Inst. Pointer     ]\n  BP: [ Base Pointer  ]                  \n  SI: [ Source Index  ]                    FLAGS: [Status & Control]\n  DI: [ Dest. Index   ]\n```\n\n### Explanation of Registers\n*   General Purpose Registers:\n    *   *AX (Accumulator Register - AH/AL)*: Used in word and byte arithmetic operations, input/output port transfers, and multiplication/division calculations.\n    *   *BX (Base Register - BH/BL)*: Holds base offset addresses for indexing memory variables. It is the only general-purpose register that can be used for indirect memory addressing.\n    *   *CX (Count Register - CH/CL)*: Acts as an automatic loop counter for the `LOOP` instruction and a shift/rotate counter for multi-bit shifts.\n    *   *DX (Data Register - DH/DL)*: Holds I/O port addresses during port instructions and stores high-order results during word multiplication/division.\n*   Segment Registers:\n    *   *CS (Code Segment)*: Holds the 16-bit base address of the code segment containing program instructions.\n    *   *DS (Data Segment)*: Points to the base address of the data segment where static variables and program data are stored.\n    *   *SS (Stack Segment)*: Holds the base address of the stack segment used for return addresses and temporary parameter storage.\n    *   *ES (Extra Segment)*: Points to the extra segment, primarily used as a destination segment for string operations.\n*   Pointer and Index Registers:\n    *   *SP (Stack Pointer)*: Stores the 16-bit offset of the top of the stack, decrementing on PUSH and incrementing on POP operations.\n    *   *BP (Base Pointer)*: Used to access local variables and parameters passed onto the stack during subroutine calls.\n    *   *SI (Source Index)*: Stores the source offset address in the data segment for string operations and index addressing.\n    *   *DI (Destination Index)*: Stores the destination offset address in the extra segment for string operations.\n*   Flag Register (Status and Control Flags):\n    *   *Status Flags (CF, PF, AF, ZF, SF, OF)*: Reflect the arithmetic or logical outcome of the last ALU operation (e.g., carry, zero result, overflow, sign status).\n    *   *Control Flags (TF, IF, DF)*: Define operating modes, including single-step trap debugging (TF), enabling maskable interrupts (IF), and string increment direction (DF).\n\n### Key Features\n*   **8-bit and 16-bit Access**: General-purpose registers can be accessed as single 16-bit registers or split into two independent 8-bit registers (High/Low) to handle byte data.\n*   **Segment Offset Cooperation**: Segments combine with dedicated offset registers (CS:IP, DS:SI, SS:SP, ES:DI) to generate 20-bit physical memory addresses."
        },
        {
          "id": "co_mp_q14",
          "number": 14,
          "question": "Explain Immediate, Register, Direct and Indirect Addressing Modes",
          "marks": 7,
          "answer": "### Definition / Introduction\nAddressing modes define the operational logic used by the CPU to locate and access operands (data) for instruction execution. The 8086 provides different addressing modes to support access to constants, registers, variables, and data structures like arrays or stack frames.\n\n### Diagram\n```\n  Immediate: [ Opcode ] [ Direct Operand Data ]\n  Register:  [ Opcode ] [ Reg Source ] ──► [ Reg Dest ]\n  Direct:    [ Opcode ] [ 16-Bit Memory Offset ] ──► RAM Address\n  Indirect:  [ Opcode ] [ Reg Pointer (BX/SI/DI) ] ──► Address in Reg ──► RAM Address\n```\n\n### Detailed Explanation of Addressing Modes\n*   Immediate Addressing Mode:\n    *   *Logic*: The operand is a constant value included directly within the instruction code. The CPU does not perform any memory cycles to fetch the data.\n    *   *Application*: Used to initialize registers with constant values or load constants for math operations.\n    *   *Example*: `MOV AX, 1234H` (Loads the 16-bit hex constant 1234H directly into Register AX).\n*   Register Addressing Mode:\n    *   *Logic*: The operand data is stored in a CPU register, and the instruction specifies the source and destination registers. It executes entirely within the CPU, requiring zero memory access cycles.\n    *   *Application*: Used for high-speed arithmetic and logic operations where operands are already loaded into registers.\n    *   *Example*: `MOV AX, BX` (Copies the 16-bit contents of register BX into register AX).\n*   Direct Addressing Mode:\n    *   *Logic*: The 16-bit offset address of the memory operand is written directly in the instruction. The CPU shifts the Data Segment (DS) register left by 4 bits and adds this displacement to find the physical address.\n    *   *Application*: Used to access static variables that have fixed locations in memory.\n    *   *Example*: `MOV AX, [5000H]` (Reads a word from the physical address calculated as (DS x 10H) + 5000H and stores it in AX).\n*   Register Indirect Addressing Mode:\n    *   *Logic*: The offset address of the memory operand is stored in a pointer or index register (BX, BP, SI, or DI). The instruction references the register in brackets, and the CPU reads the address from that register.\n    *   *Application*: Used to iterate through arrays, loops, and data blocks where memory offsets change dynamically during execution.\n    *   *Example*: `MOV AX, [SI]` (Reads a word from the physical address calculated as (DS x 10H) + SI and stores it in AX).\n\n### Key Features\n*   **Flexible Access**: Allows programs to access constants, registers, static memory locations, or dynamic arrays.\n*   **Effective Address Generation**: Combines segment bases with index values, base values, and displacements to support complex data structures."
        },
        {
          "id": "co_mp_q15",
          "number": 15,
          "question": "How is 8086 Instruction Set Classified? Explain Any Two Categories",
          "marks": 6,
          "answer": "### Definition / Introduction\nThe 8086 instruction set consists of the machine commands that the CPU can execute. These instructions are classified into six categories based on their function, allowing programmers to perform data movement, mathematical calculations, logical processing, program branching, and system state control.\n\n### Diagram\n```\n                          ┌──────────────────────────┐\n                          │   8086 Instruction Set   │\n                          └────────────┬─────────────┘\n          ┌──────────────┬─────────────┼──────────────┬──────────────┐\n          ▼              ▼             ▼              ▼              ▼\n     ┌────────┐     ┌────────┐    ┌────────┐     ┌────────┐     ┌────────┐\n     │Data    │     │Arith-  │    │Logical │     │Program │     │Proces- │\n     │Transfer│     │metic   │    │& Shift │     │Control │     │sor Ctrl│\n     └────────┘     └────────┘    └────────┘     └────────┘     └────────┘\n```\n\n### Detailed Explanation of Categories\n*   Data Transfer Category:\n    *   *Purpose*: Used to copy bytes or words of data between registers, memory locations, and I/O ports. These instructions do not modify the data value.\n    *   *Flag Impact*: None of the status flags in the flag register are affected by data transfer operations.\n    *   *Instructions*: Includes `MOV` (move data), `PUSH`/`POP` (stack access), `IN`/`OUT` (I/O port access), and `XCHG` (exchange register values).\n    *   *Examples*:\n        *   `MOV AX, [SI]` (Copies a 16-bit word from the memory location pointed to by SI into Register AX).\n        *   `PUSH BX` (Decrements the stack pointer SP by 2 and writes the contents of BX onto the stack).\n*   Arithmetic Category:\n    *   *Purpose*: Performs binary, packed BCD, or unpacked BCD mathematical calculations on registers or memory data.\n    *   *Flag Impact*: Updates the status flags (CF, PF, AF, ZF, SF, OF) to record the result of the operation.\n    *   *Instructions*: Includes `ADD`/`SUB` (addition/subtraction), `MUL`/`DIV` (multiplication/division), `INC`/`DEC` (increment/decrement), and `CMP` (compare by subtraction).\n    *   *Examples*:\n        *   `ADD AX, BX` (Adds the contents of BX to AX, writes the sum to AX, and updates status flags).\n        *   `CMP AL, 05H` (Subtracts 05H from AL to update status flags, leaving AL unmodified).\n*   Other Categories (Logical, Branch, and Control):\n    *   *Logical*: Performs Boolean logic operations (AND, OR, XOR, NOT) and shifts/rotates bit values.\n    *   *Program Control*: Manages program branching using conditional/unconditional jumps, subroutine calls, and loops.\n    *   *Processor Control*: Modifies CPU flag states (e.g., CLC to clear carry) and coordinates bus operations (e.g., LOCK, HLT).\n\n### Key Features\n*   **Flag Management**: Data transfer instructions preserve the flag register, while arithmetic and logical operations update flag status to allow conditional branching.\n*   **Variable Data Sizes**: Most instructions support operations on both 8-bit bytes and 16-bit words."
        }
      ]
    },
    {
      "unitNum": 4,
      "unitTitle": "Memory Organization and Interrupts",
      "questions": [
        {
          "id": "co_mp_q16",
          "number": 16,
          "question": "Explain Even and Odd Memory Banks of 8086",
          "marks": 7,
          "answer": "### Definition\nThe 8086 microprocessor features a 20-bit address bus and a 16-bit data bus, allowing it to address up to 1 MB of physical memory. To enable 16-bit word access in a single bus cycle while maintaining byte-addressability, the memory is physically divided into two parallel blocks of 512 KB: the Even Memory Bank (Lower Bank) and the Odd Memory Bank (Upper Bank).\n\n### Diagram\n```\n                          8086 CPU\n               ┌─────────────────────────┐\n               │    16-bit Data Bus       │\n               │  D0-D7      D8-D15      │\n               └────┬────────────┬───────┘\n                    │            │\n            A0=0     │            │    BHE̅=0\n          (select)   │            │   (select)\n                    ▼            ▼\n          ┌──────────────┐ ┌──────────────┐\n          │  EVEN BANK   │ │  ODD BANK    │\n          │ (Lower Bank) │ │ (Upper Bank) │\n          │              │ │              │\n          │ Addr 00000H  │ │ Addr 00001H  │\n          │ Addr 00002H  │ │ Addr 00003H  │\n          │     ...      │ │     ...      │\n          │  512 KB      │ │  512 KB      │\n          └──────────────┘ └──────────────┘\n```\n\n### Components and Functions\n*   Even Memory Bank (Lower Bank): Stores all data bytes located at even physical addresses (e.g., 00000H, 00002H, etc.). It is connected directly to the lower half of the CPU data bus, lines D0–D7, and is enabled when address line A0 is driven low (A0 = 0).\n*   Odd Memory Bank (Upper Bank): Stores all data bytes located at odd physical addresses (e.g., 00001H, 00003H, etc.). It is connected to the upper half of the data bus, lines D8–D15, and is enabled when the active-low control signal BHE̅ (Bus High Enable) is driven low (BHE̅ = 0).\n*   Address Line A0 Pin: Acts as the hardware chip-select signal for the Even Bank. When the CPU executes an operation at an even address, A0 is driven low, enabling data transfer over lines D0–D7.\n*   BHE̅ (Bus High Enable) Pin: Acts as the hardware chip-select signal for the Odd Bank. When driven low, it enables the data transceivers for the upper data bus lines (D8–D15), allowing byte access to odd memory locations.\n\n### Working of Memory Banks\nThe CPU coordinates A0 and BHE̅ to perform three types of memory accesses:\n1. **Aligned Word Access (Single Bus Cycle)**: Occurs when the CPU reads or writes a 16-bit word starting at an even memory address (e.g., 2000H). The CPU drives A0 = 0 and BHE̅ = 0 simultaneously, activating both banks. The lower byte at 2000H is read over D0–D7, and the upper byte at 2001H is read over D8–D15 in one cycle.\n2. **Unaligned Word Access (Two Bus Cycles)**: Occurs when the CPU accesses a 16-bit word starting at an odd address (e.g., 2001H). \n   * In *Cycle 1*, the CPU reads the odd byte at 2001H by setting A0 = 1 and BHE̅ = 0, transferring the byte over D8–D15.\n   * In *Cycle 2*, the CPU increments the address to the next even location 2002H, setting A0 = 0 and BHE̅ = 1 to transfer the remaining byte over D0–D7.\n3. **Byte Access**: For an even byte address, the CPU sets A0 = 0 and BHE̅ = 1, transferring data over D0–D7. For an odd byte address, it sets A0 = 1 and BHE̅ = 0, transferring data over D8–D15.\n\n### Advantages / Features\n*   **16-Bit Parallel Access**: Enables fetching a full 16-bit word in a single bus cycle, doubling the data transfer rate compared to 8-bit memory designs.\n*   **Byte-Addressable Space**: Maintains backward compatibility with 8-bit peripherals by allowing individual byte transfers over D0–D7 or D8–D15."
        },
        {
          "id": "co_mp_q17",
          "number": 17,
          "question": "Read and Write Timing Diagram of 8086",
          "marks": 7,
          "answer": "### Definition / Introduction\nA bus cycle is the sequence of events performed by the 8086 processor to read or write data to/from external memory or I/O devices. One basic bus cycle consists of four clock periods (T-states): T1, T2, T3, and T4, which coordinate address latching, data direction control, and read/write operations.\n\n### Diagrams\n```\n  READ CYCLE:\n          T1        T2        T3        T4\n       ┌──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐\n CLK   │  │  │  │  │  │  │  │  │  │  │  │  │  │  │  │\n     ──┘  └──┘  └──┘  └──┘  └──┘  └──┘  └──┘  └──┘  ───\n       ┌─────┐\n ALE   │     │\n     ──┘     └─────────────────────────────────────────\n     ──┬───────────┬──────────────────────┬────────────\n AD   │  ADDRESS  │◄──── DATA IN ────────│\n 0-15 │ (A0-A15)  │  (from memory)       │\n     ──┴───────────┴──────────────────────┴────────────\n                   ┌─────────────────────┐\n RD̅   ─────────────┘                     └─────────────\n       (HIGH)         (LOW = Active)        (HIGH)\n\n  WRITE CYCLE:\n          T1        T2        T3        T4\n      ──┬───────────┬──────────────────────┬───────────\n AD   │  ADDRESS  │◄── DATA OUT ─────────│\n 0-15 │ (A0-A15)  │ (from CPU to memory) │\n      ──┴───────────┴──────────────────────┴───────────\n                   ┌─────────────────────┐\n WR̅   ─────────────┘                     └─────────────\n       (HIGH)         (LOW = Active)        (HIGH)\n```\n\n### Detailed Explanation of T-States\n*   **State T1 (Address Phase)**: The CPU places the 20-bit physical address on the multiplexed AD0–AD15 and status lines. It drives the ALE (Address Latch Enable) signal high to latch the address into external registers, and configures the M/IO̅ and DT/R̅ signals to define the cycle type and data direction.\n*   **State T2 (Bus Turnaround/Control)**: The ALE signal goes low to lock the address in external latches. For read cycles, the multiplexed bus lines are put into a high-impedance state to allow the memory device to drive the bus. For write cycles, the CPU drives the data onto the bus lines. The appropriate control signal (RD̅ or WR̅) and DEN̅ are driven low to enable the data transceivers.\n*   **State T3 (Data Phase)**: The CPU reads the byte/word from the data bus (during read cycles) or holds the data stable (during write cycles). At the start of T3, the CPU samples the READY pin; if READY is low, the CPU inserts Wait States (Tw) between T3 and T4, holding all signals active until READY goes high.\n*   **State T4 (Termination Phase)**: The control signals (RD̅ or WR̅) and DEN̅ are driven high, disabling the data transceivers. The CPU stops driving the data bus (during write cycles) or stops reading the bus (during read cycles), returning the AD0–AD15 lines to a high-impedance state.\n\n### Advantages / Features\n*   **Supports Slow Peripherals**: Uses the READY pin to dynamically insert wait states (Tw) when accessing slow memory or I/O devices.\n*   **Bus Isolation**: Uses the DEN̅ signal to isolate the CPU's internal data bus from the system bus, preventing collisions during address phases."
        },
        {
          "id": "co_mp_q18",
          "number": 18,
          "question": "Compare Memory-Mapped I/O and I/O-Mapped I/O",
          "marks": 8,
          "answer": "| Comparison Parameter | Memory-Mapped I/O | I/O-Mapped I/O |\n| :--- | :--- | :--- |\n| **1. Address Space Partition** | Shares the same physical address space as system RAM. | Occupies a completely separate, isolated address space. |\n| **2. Maximum Port Capacity** | Limited only by total system RAM (up to 1 MB in 8086). | Restricted to a dedicated range of 64 KB (65,536 ports). |\n| **3. Control Signal Select** | Relies on memory control signals like MEMR̅ and MEMW̅. | Uses specialized I/O control signals via the M/IO̅ line. |\n| **4. Instruction Set Support** | Can use any memory reference instructions (e.g., `MOV`, `ADD`). | Restricted to specialized input/output instructions (`IN`/`OUT`). |\n| **5. Register Access** | Allows transferring port data to and from any CPU register. | Restricts data transfers strictly to the accumulator (AL/AX). |\n| **6. Hardware Complexity** | Requires complex decoders to detect 20-bit memory addresses. | Uses simple, cheaper decoders to decode 16-bit I/O addresses. |\n| **7. Transfer Speed** | Faster due to memory-access optimizations. | Slower due to dedicated instruction execution overhead. |\n| **8. Memory Space Penalty** | Reduces available RAM space by reserving addresses for ports. | Preserves the entire 1 MB address space for system RAM. |"
        },
        {
          "id": "co_mp_q19",
          "number": 19,
          "question": "Calculate Physical Address",
          "marks": 4,
          "answer": "### Introduction / Definition\nThe 8086 microprocessor uses a segmented memory model to generate 20-bit physical addresses using 16-bit registers. The physical memory is divided into logical segments of 64 KB, and the physical address is calculated by combining a 16-bit Segment Address and a 16-bit Offset Address.\n\n### Formula\nPhysical Address = (Segment Address · 10H) + Offset Address\n\n### Detailed Step-by-Step Examples\n*   Code Segment Calculation (CS:IP):\n    *   *Parameters*: Segment CS = `2000H`, Offset IP = `1234H`.\n    *   *Calculation*: The CPU shifts the segment address left by 4 bits (equivalent to multiplying by 10H) to obtain `20000H`. It then adds the offset address `1234H`.\n    *   *Step 1*: 2000H · 10H = 20000H\n    *   *Step 2*: 20000H + 1234H = 21234H\n    *   *Result*: Physical Address = `21234H`.\n*   Data Segment Calculation (DS:SI):\n    *   *Parameters*: Segment DS = `1500H`, Offset SI = `0200H`.\n    *   *Calculation*: The CPU shifts the data segment base to obtain `15000H` and adds the source index offset `0200H`.\n    *   *Step 1*: 1500H · 10H = 15000H\n    *   *Step 2*: 15000H + 0200H = 15200H\n    *   *Result*: Physical Address = `15200H`.\n*   Stack Segment Calculation (SS:SP):\n    *   *Parameters*: Segment SS = `3000H`, Offset SP = `0100H`.\n    *   *Calculation*: The CPU shifts the stack segment base to obtain `30000H` and adds the stack pointer offset `0100H`.\n    *   *Step 1*: 3000H · 10H = 30000H\n    *   *Step 2*: 30000H + 0100H = 30100H\n    *   *Result*: Physical Address = `30100H`.\n*   Extra Segment Calculation (ES:DI):\n    *   *Parameters*: Segment ES = `4000H`, Offset DI = `00A0H`.\n    *   *Calculation*: The CPU shifts the extra segment base to obtain `40000H` and adds the destination index offset `00A0H`.\n    *   *Step 1*: 4000H · 10H = 40000H\n    *   *Step 2*: 40000H + 00A0H = 400A0H\n    *   *Result*: Physical Address = `400A0H`.\n\n### Advantages / Features\n*   **Enables 1 MB Addressing**: Allows a processor with 16-bit internal registers to generate 20-bit addresses for a 1 MB physical memory space.\n*   **Code Relocatability**: Programs can be moved to different physical memory areas by changing the segment base registers without modifying offsets."
        },
        {
          "id": "co_mp_q20",
          "number": 20,
          "question": "Explain Interrupt Handling Mechanism and IVT",
          "marks": 6,
          "answer": "### Introduction / Definition\nAn interrupt is an electrical signal that temporarily suspends the execution of the main program, forcing the CPU to save its state and branch to an Interrupt Service Routine (ISR). The Interrupt Vector Table (IVT) is a dedicated lookup table in memory that stores the starting addresses of the ISRs.\n\n### Diagram\n```\n  [Interrupt Request] ──► PUSH Flags/CS/IP ──► Load New CS:IP ──► Execute ISR\n```\n\n### Detailed Explanation of Interrupt Mechanism\n*   **Instruction Completion**: The CPU completes execution of the currently running machine instruction before servicing the interrupt request to prevent data corruption.\n*   **Push Flags onto Stack**: The CPU decrements the Stack Pointer (SP) by 2 and pushes the 16-bit Flag Register onto the stack to preserve the status flags.\n*   **Clear IF and TF Flags**: The CPU clears the Interrupt Flag (IF) to 0 to disable further maskable interrupts, and clears the Trap Flag (TF) to 0 to disable single-step debugging during the ISR.\n*   **Push CS and IP onto Stack**: The CPU decrements SP by 4 and pushes the current Code Segment (CS) and Instruction Pointer (IP) registers onto the stack to save the return address.\n*   **IVT Address Calculation**: The CPU multiplies the interrupt type number `n` by 4 to locate the vector entry in the table:\n    IVT Address = n · 4\n*   **Fetch CS:IP Vector**: The CPU reads the first 2 bytes from the calculated IVT address and loads them into the IP register (offset), and reads the next 2 bytes to load into the CS register (segment base).\n*   **Execute ISR**: The CPU jumps to the new CS:IP address and executes the Interrupt Service Routine instructions.\n*   **Return to Main Program**: The ISR ends with the `IRET` instruction, which pops the saved IP, CS, and Flag values off the stack, resuming the main program.\n\n### Detailed Structure of IVT\n*   **Memory Location**: Stored in the lowest 1024 bytes of physical memory, spanning from address `00000H` to `003FFH`.\n*   **Capacity**: Supports 256 vector addresses (interrupt types 0 to 255), with each entry occupying 4 bytes (2 bytes for IP, 2 bytes for CS).\n*   Interrupt Predefined Types:\n    *   *Type 0 (Divide by Zero)*: Triggered if a division operation results in an overflow.\n    *   *Type 1 (Single Step)*: Executed after every instruction if the Trap Flag (TF) is set.\n    *   *Type 2 (NMI)*: Triggered by a signal on the Non-Maskable Interrupt pin.\n    *   *Type 3 (Breakpoint)*: Triggered by the `INT 3` instruction for debugging.\n    *   *Type 4 (Overflow)*: Triggered by the `INTO` instruction if the Overflow Flag (OF) is set.\n\n### Key Features\n*   **Fast Vector Lookup**: Calculates the target ISR address using simple multiplication (n x 4), reducing interrupt latency.\n*   **Priority Management**: Coordinates hardware interrupts (via INTR, NMI) and software interrupts (via INT n) based on internal priority rules."
        }
      ]
    },
    {
      "unitNum": 5,
      "unitTitle": "Parallel Organization",
      "questions": [
        {
          "id": "co_mp_q21",
          "number": 21,
          "question": "Closely Coupled and Loosely Coupled Multiprocessor Systems",
          "marks": 6,
          "answer": "| Comparison Parameter | Closely Coupled System | Loosely Coupled System |\n| :--- | :--- | :--- |\n| **1. Memory Structure** | Processors share a single centralized main memory block or common memory pool. | Each processor has its own private, distributed local memory block. |\n| **2. Processor Communication** | Through shared variables and semaphores in the common memory. | Through message packets sent over a communication network. |\n| **3. Data Transfer Rate** | Extremely high, limited only by the memory bus bandwidth. | Moderate to low, limited by network routing and link bandwidth. |\n| **4. Operating System Layout** | A single operating system kernel controls all processors and hardware resources. | Each processor node runs its own independent copy of the OS. |\n| **5. System Scaling Limits** | Low scalability because the shared memory bus becomes a bottleneck as processors are added. | High scalability since independent nodes can be added without bus conflicts. |\n| **6. Hardware Fault Impact** | Lower fault tolerance; a failure in the shared memory or system bus crashes the entire system. | Higher fault tolerance; a failed node can be isolated without affecting others. |\n| **7. Synchronization** | Relies on hardware locks or shared memory semaphores to coordinate access. | Uses software message-passing protocols to synchronize nodes. |\n| **8. Physical Distance** | Very short, typically within a single backplane or chassis board. | Can be long, distributed across multiple rack units or network segments. |\n| **9. Arbitration Controls** | Requires complex bus arbitration logic to manage shared memory bus access. | Not required since nodes access private memories over local buses. |\n| **10. Typical Applications** | Symmetric multiprocessors (SMPs) and high-performance multi-core processors. | Cluster computing, grid computing, and distributed server farms. |"
        },
        {
          "id": "co_mp_q22",
          "number": 22,
          "question": "Explain SMP Organization",
          "marks": 7,
          "answer": "### Definition\nSymmetric Multiprocessing (SMP) is a multiprocessor hardware architecture where two or more identical processors connect to a single, shared main memory and shared I/O devices. It is symmetric because all processors have equal rights to access resources, and no processor acts as a master while others act as slaves.\n\n### Labelled Diagram\n```\n  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐\n  │   Processor 1 │ │   Processor 2 │ │   Processor n │\n  │  ┌─────────┐  │ │  ┌─────────┐  │ │  ┌─────────┐  │\n  │  │ Cache   │  │ │  │ Cache   │  │ │  │ Cache   │  │\n  │  └────┬────┘  │ │  └────┬────┘  │ │  └────┬────┘  │\n  └───────┼───────┘ └───────┼───────┘ └───────┼───────┘\n          │                 │                 │\n  ════════╪═════════════════╪═════════════════╪═════════\n                      Shared System Bus\n  ════════╪═══════════════════════════════════╪═════════\n          ├───────────────────────────────────┘\n          ▼\n    ┌─────────────┐       ┌─────────────┐\n    │ SHARED RAM  │       │ SHARED I/O  │\n    └─────────────┘       └─────────────┘\n```\n\n### Explanation of SMP Components\n*   **Identical CPU Cores**: These are the physical execution units that run threads in parallel. They share the same instruction set and architecture, allowing the operating system to distribute tasks dynamically.\n*   **Private Caches**: High-speed caches (L1/L2) associated with each processor to reduce bus accesses and memory read latency. They store copies of recently accessed main memory blocks.\n*   **Shared System Bus**: The common address, data, and control bus that connects all processors, main memory, and I/O controllers.\n*   **Shared RAM**: A single physical address space that is accessible by all processors. It holds the operating system code, active program data, and shared variables.\n*   **Shared I/O Controllers**: Input/output controllers that are shared by all processors, allowing any processor to initiate disk or network transfers.\n\n### Cache Consistency Working (Bus Snooping and MESI)\nTo prevent processors from reading stale data from their private caches, SMP systems use cache consistency protocols:\n1. **Bus Snooping**: Cache controllers continuously monitor (\"snoop\") the shared system bus. If a processor writes to a memory location that exists in another processor's cache, the snooping controller invalidates or updates the local copy.\n2. **MESI Protocol**: Each cache line is marked with one of four states:\n   * *M (Modified)*: The cache line has been modified by the local CPU and is the only valid copy in the system (main memory is stale).\n   * *E (Exclusive)*: The cache line matches main memory and is present only in the local cache.\n   * *S (Shared)*: The cache line matches main memory and is present in other caches.\n   * *I (Invalid)*: The cache line contains old data and must be re-read from memory.\n\n### Key Features\n*   **Load Balancing**: The operating system kernel schedules active threads across any available idle processor, maximizing throughput.\n*   **Graceful Degradation**: If a processor fails, the system remains operational, disabling the failed core and continuing on the working ones."
        },
        {
          "id": "co_mp_q23",
          "number": 23,
          "question": "Explain Flynn's Taxonomy",
          "marks": 7,
          "answer": "### Definition / Introduction\nFlynn's Taxonomy is a classification system that categorizes computer architectures based on the number of concurrent **Instruction Streams** and **Data Streams** active in the processor. It divides computers into four classes: SISD, SIMD, MISD, and MIMD.\n\n### Diagrams\n```\n  SISD:  [ Memory ] ──► [ Control Unit ] ──► [ Processor Element ]\n  \n  SIMD:                 ┌──────────────┐\n                        │ Control Unit │\n                        └──────┬───────┘\n                               │ Instruction Stream (Broadcast)\n                 ┌─────────────┼─────────────┐\n                 ▼             ▼             ▼\n            ┌─────────┐   ┌─────────┐   ┌─────────┐\n            │  PE 1   │   │  PE 2   │   │  PE n   │\n            └─────────┘   └─────────┘   └─────────┘\n\n  MISD:     ┌─────────┐   ┌─────────┐   ┌─────────┐\n            │  CU 1   │   │  CU 2   │   │  CU n   │\n            └────┬────┘   └────┬────┘   └────┬────┘\n                 ▼            ▼            ▼\n   Data ──► ┌─────────┐ ──► ┌─────────┐ ──► ┌─────────┐ ──►\n   Stream   │  PE 1   │     │  PE 2   │     │  PE n   │\n            └─────────┘     └─────────┘     └─────────┘\n\n  MIMD:      ┌─────────┐     ┌─────────┐     ┌─────────┐\n             │  Node 1 │     │  Node 2 │     │  Node n │\n             │ ┌───┐   │     │ ┌───┐   │     │ ┌───┐   │\n             │ │CPU│   │     │ │CPU│   │     │ │CPU│   │\n             │ └───┘   │     │ └───┘   │     │ └───┘   │\n             └────┬────┘     └────┬────┘     └────┬────┘\n                  └───────────────┼───────────────┘\n                                  ▼\n                        Interconnection Network\n```\n\n### Detailed Explanation of Flynn's Classes\n*   SISD (Single Instruction, Single Data):\n    *   *Operation*: A single control unit fetches one instruction stream from memory and executes it on a single data stream using one processor element.\n    *   *Characteristics*: Standard sequential computer architecture. Execution is serialized, and there is no hardware-level parallelism.\n    *   *Example*: Older single-core PCs and microcontrollers.\n*   SIMD (Single Instruction, Multiple Data):\n    *   *Operation*: A single control unit fetches one instruction and broadcasts it to multiple processing elements. Each processing element executes the same instruction on its own distinct data.\n    *   *Characteristics*: Synchronous parallel architecture where all processing units work in lockstep.\n    *   *Example*: Graphics Processing Units (GPUs) and vector processors.\n*   MISD (Multiple Instruction, Single Data):\n    *   *Operation*: Multiple control units fetch different instructions, and multiple processing elements execute them simultaneously on the same single data stream.\n    *   *Characteristics*: Theoretical architecture, rarely used in general computing. Mainly used for redundant, fault-tolerant configurations.\n    *   *Example*: Space shuttle flight controllers and backup system computers.\n*   MIMD (Multiple Instruction, Multiple Data):\n    *   *Operation*: Multiple independent processors fetch their own instructions and execute them on their own data streams.\n    *   *Characteristics*: Asynchronous parallel architecture. The processors can communicate via shared memory or message-passing networks.\n    *   *Example*: Modern multi-core processors, supercomputers, and server clusters.\n\n### Key Features\n*   **Concurrency Levels**: Flynn's taxonomy classifies systems from zero hardware concurrency (SISD) to maximum task-level concurrency (MIMD).\n*   **Hardware Complexity**: SIMD requires a single decoder but multiple ALUs, while MIMD requires duplicate control units, ALUs, and bus interfaces."
        },
        {
          "id": "co_mp_q24",
          "number": 24,
          "question": "Compare UMA and NUMA Architectures",
          "marks": 8,
          "answer": "| Comparison Parameter | UMA Architecture | NUMA Architecture |\n| :--- | :--- | :--- |\n| **1. Memory Layout** | Centralized memory pool shared equally by all processors. | Distributed physical memory adjacent to each processor node. |\n| **2. Memory Access Delay** | Uniform access time; delay is constant regardless of location. | Non-uniform access time; local memory is faster than remote memory. |\n| **3. Interconnection** | Uses system bus, crossbar switch, or multistage network. | Relies on high-speed point-to-point networks or router links. |\n| **4. System Scalability** | Low scalability, typically limited to fewer than 32 processors. | High scalability, scaling to thousands of processors. |\n| **5. Cache Consistency** | Relies on bus snooping (e.g., MESI) over the shared bus. | Uses directory-based cache consistency to avoid bus traffic. |\n| **6. Memory Bandwidth** | Low total bandwidth because all processors share a single bus. | High total bandwidth because each processor has a private bus. |\n| **7. Controller Complexity** | Simpler memory controller design and standard memory mapping. | Highly complex directory and routing logic inside memory controllers. |\n| **8. Performance Bottleneck** | Bus conflicts or memory controller bottlenecks. | Network delays and latency during remote memory access cycles. |\n| **9. Typical Application** | Personal computers, workstation multi-core processors. | High-performance computing clusters and massive database servers. |"
        },
        {
          "id": "co_mp_q25",
          "number": 25,
          "question": "Compare RISC and CISC Architectures",
          "marks": 7,
          "answer": "| Comparison Parameter | RISC Architecture | CISC Architecture |\n| :--- | :--- | :--- |\n| **1. Instruction Set Type** | Small set of simple, single-cycle instructions. | Large set of complex, variable-cycle instructions. |\n| **2. Instruction Word Length** | Fixed-size instruction words (typically 32 bits). | Variable-size instruction words (1 to 15 bytes). |\n| **3. Internal Register File** | Large register file (typically 32 or more registers). | Small register file (typically 8 to 16 registers). |\n| **4. Control Unit Design** | Hardwired control unit using combinational logic. | Microprogrammed control unit relying on microcode ROM. |\n| **5. Memory Reference Model** | Strict Load-Store architecture; only LOAD/STORE access memory. | Memory-to-memory architecture; operations can directly access RAM. |\n| **6. Pipelining Compatibility** | Highly compatible with deep pipelining. | Difficult to pipeline efficiently due to variable execution times. |\n| **7. Compiler Responsibility** | High compiler optimization required to schedule registers. | Simpler compiler design since hardware implements complex operations. |\n| **8. Addressing Modes** | Supports a limited number of simple addressing modes. | Features a wide variety of complex addressing modes. |\n| **9. Physical Chip Area** | Saves chip area, leaving more space for registers. | Reserves significant chip area for microcode ROM. |\n| **10. Typical Architecture** | ARM, RISC-V, MIPS. | Intel x86, AMD. |"
        }
      ]
    }
  ]
};
